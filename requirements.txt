Django==5.2.14
djangorestframework==3.17.1

# API and auth
djangorestframework-simplejwt==5.5.1
django-filter==24.3
django-allauth==65.16.1
google-auth==2.52.0
requests==2.34.1

# Cross-origin requests for frontend apps
django-cors-headers==4.9.0

# Environment and config
python-dotenv==1.2.2
django-environ==0.13.0

# Database and media
psycopg2-binary==2.9.12
Pillow==12.2.0

# Object storage (S3) for media/static in production
django-storages==1.14.6
boto3==1.40.15

# Async processing + queue
celery==5.6.3
redis==5.3.1

# Real-time WebSocket
channels==4.3.2
channels-redis==4.3.0
daphne==4.2.2

# Video processing
ffmpeg-python==0.2.0

# Code execution sandbox (Docker SDK for the coding-exercise runner)
docker==7.1.0

# Production server
gunicorn==22.0.0

# Certificate PDF generation
reportlab==4.5.1

# Admin-console device/session tracking: parse User-Agent → browser/os/device
user-agents==2.2.0

# CloudFront-signed HLS URLs (RSA-SHA1 signing of the canned policy).
cryptography==44.0.0
